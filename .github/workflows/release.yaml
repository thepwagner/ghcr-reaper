name: Release

on:
  push:
    tags: ["v*.*.*"]

permissions: {}

jobs:
  build:
    uses: thepwagner-org/actions/.github/workflows/golang-release-attest.yaml@github-attestations
    permissions:
      contents: write
      id-token: write
      attestations: write
